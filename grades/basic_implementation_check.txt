in main.R:

1. the description could be better. you currently have:

#' @param respond A string contains selecting variable name.
#' @param predictor A string contains selecting variable name.

I would explicitly say that `respond` is a strong that contains the response variable (not just the generic 
"selecting variable name"). Same goes with `predictor`.

2. in general, I would discourage using the boot package (although I did not explicitly speak against using it, so it
would not be penalized against you).

This is because part of the learning goal is for you to know how to code the bootstrap yourself to understand it better.
So therefore, using a package that someone else is using somewhat defeats the purpose.

3. graphics is included in base R installation so you don't need to do something like:
graphics::hist

this is also something I've never done during lecture... you are using the same hist() function that I have always used.

the same rule applies for stats. You don't need to use:
stats::IQR

in test_bootstrap_boston.R:

you used expect_error( some_function, NA)

if you expect no error, you should be using expect_no_error.
https://testthat.r-lib.org/reference/expect_no_error.html

I fixed it in a couple of places but you should fix it for the rest.

vignette:

there's a typo for the file name.

great use of references and referring to what AI was used for.

for assumption checking, a good package is performance. in particular, performance::check_model() would give good plots,
however, it also includes the posterior predictive checks (which is not relevant in our case).

https://easystats.github.io/performance/reference/check_model.html

2/2




